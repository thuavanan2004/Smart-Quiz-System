<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
      Structured logging theo design §14.3.
        - Dev: console text có requestId cho đọc nhanh.
        - Khác (staging/prod): JSON stdout → Loki/ELK parse.
      MDC keys: requestId (từ RequestIdFilter), userId (nếu có — auth handler inject vào MDC khi
      principal đã resolve).
    -->

    <springProperty name="serviceName" source="spring.application.name" defaultValue="auth-service"/>

    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{requestId:-}] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="vn.smartquiz" level="DEBUG"/>
    </springProfile>

    <springProfile name="!dev">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <includeMdcKeyName>orgId</includeMdcKeyName>
                <customFields>{"service":"${serviceName}"}</customFields>
                <fieldNames>
                    <timestamp>ts</timestamp>
                    <message>msg</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
        <logger name="vn.smartquiz" level="INFO"/>
    </springProfile>
</configuration>
