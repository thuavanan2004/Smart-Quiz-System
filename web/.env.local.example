# Web dev env (Iron Rule #4: config qua env, không hardcode URL).
# Copy sang .env.local khi dev local — file này commit, .env.local KHÔNG commit.

# Auth Service base URL (không trailing slash).
NEXT_PUBLIC_AUTH_API_URL=http://localhost:3001
